# Never commit a list.
#
# Every file these scripts produce names real businesses and often real people,
# and this repo is written as though it were public (ops/audit-method.md
# section 5). A prospect list is also personal data the day it is written
# (ops/client-record.md). The --out path should be outside the repo entirely;
# this is the second guard.
*.csv
*.xlsx
*.json
